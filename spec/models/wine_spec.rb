require 'spec_helper'

describe 'Wines' do

  it "has a valid factory"
   it "is invalid without a name"
   it "is invalid without a unique name"
   it "is invalid without a unique url"
   it "is invalid without min price"
   it "is invalid without a max price"
   it "is invalid without a retail price"
   it "is invalid if min price is greater than max price"
end
